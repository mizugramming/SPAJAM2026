# 共同開発ルールブック

このファイルはアプリ名・技術スタック・SDKの数値・担当フォルダに依存しない共通ルールです。別のプロジェクトでもそのまま使えます。プロジェクト固有の情報はREADMEと、そこから参照する設計書・環境設定に置きます。
回答は日本語で簡潔に。今回の依頼に必要な範囲を確実に完了させ、未検証のことを断定しません。

## 開始時に読むもの

1. このAGENTS.md。
2. ルートのREADME：設計書の場所、現在の実装、担当範囲、統合先、実行・検証方法を確認。READMEがない場合は、提供された設計書と依頼から同じ情報を確認する。
3. ユーザーから渡された設計書、およびREADMEが案内する設計・接続・開発環境の資料。
4. 変更対象ディレクトリに追加の指示があれば、その内容。
5. 実際のファイル一覧と担当コード・接続先・関連テスト。READMEが案内する構成図と照合し、図だけからファイルの存在や責務を推測しない。生成物やキャッシュを全件読む必要はない。

「どう開発するか」はこのルールブック、「何を作るか」は今回の設計書とユーザーの依頼から判断します。新しいアプリだからといって、このファイルへアプリの仕様を書き足さないでください。
最新の明示的なユーザー指示を優先します。既に許可された範囲を繰り返し確認せず、必要な情報が資料・会話から特定できない場合だけ質問します。古い設計と現在の実装の差を無断で巻き戻しません。

## 作業範囲

- `git status --short --branch` でブランチと既存の変更を確認。他人の作業・未コミット変更を上書きしない。
- 今回の担当と変更対象をREADME・設計書・ユーザーの依頼から特定し、目的と対象を短く伝える。
- 開発者は自分の担当を把握している前提で、会話に担当・変更内容がなければ自然な言葉で質問する。プロンプトの穴埋めやファイルパスの記入を求めず、回答を実ファイルへ対応付ける。既知の内容は聞き直さず、ブランチ名などだけで担当を決めない。
- 着手前に担当範囲・変更予定ファイル・利用する共通接続・検証方法を短く示す。範囲が明確なら確認待ちにせず続行し、担当の重複や仕様矛盾など判断が必要な点だけ質問する。
- 同じ画面を複数人で分担する場合は、部品・ファイル単位で変更担当を決める。親画面への組み込み担当も決め、全員が同じ親ファイルを編集しない。
- 担当外ファイルの読み取りはよい。変更は依頼された担当のソース・素材・テストに限定する。
- 共通部分の変更が必要なら、理由・対象・影響を説明し、プロジェクトの統合担当と調整する。今回の依頼で既にその変更が許可されていれば進めてよい。
- SDK・依存関係・ロックファイル・CI・プラットフォーム設定・共通API・共有テスト・開発ルールは共通管理として扱う。画面実装や自分の環境エラーを理由に無断変更しない。
- 統合先・リリース先では直接作業せず、作業ブランチを使う。ブランチ名・PRの宛先はREADMEから確認し、推測しない。

## 環境と依存関係

- バージョンはREADMEとリポジトリの機械可読な設定を参照する。このルールブックへ数値を転記しない。
- 指定のバージョン管理ツール、環境チェック、ロックを守る依存取得コマンドを使う。CIなど別の起動方法でも、同じバージョンであることを確認する。
- 異なるSDKで動かすためにSDK制約を緩めたり、依存関係を更新したりしない。まず手元の環境をプロジェクトへ合わせる。
- 無断のSDK・パッケージ一括アップグレード、ロックファイル削除、プロジェクト全体の再生成は禁止。
- SDK本体、キャッシュ、生成物、端末固有パス、鍵・トークンをコミットしない。
- バージョン変更が依頼された場合は、環境設定・依存関係・CI・案内を一貫して更新し、全体を検証する。

## 実装と接続

- 設計書と接続資料に定義された共通モデル・状態管理・保存処理を使う。同じデータの独自コピーや別の保存先を勝手に作らない。
- 保存形式、ID、公開API、ルート、モジュール間の依存方向を無断で変更しない。
- データの追加・削除・再読み込み・再起動後も整合することを確認する。失敗時の入力保持、二重実行防止、ユーザー確認など既存の保護を維持する。
- 対象端末・画面サイズ・操作方法・アクセシビリティはREADMEが指定する本番設計書に従う。雛形の未記入欄を理由に確定済み条件を変更しない。
- 基盤の画面構造も対象端末を前提に作り、共通レイアウトから各画面へ表示領域を渡す。各担当やAIが画面全体の幅・高さ、縦横比、表示倍率、向き制御、共通パディングを独自に固定・変更しない。変更が必要なら理由・対象・全画面への影響を示し、ユーザーの許可を得る。既に許可された範囲は再確認しない。
- 物理解像度をレイアウトの論理寸法として使わない。OS・キーボード・文字拡大による利用可能領域の変化には追従し、はみ出しを画面拡張や文字縮小で隠さない。
- 目的と関係のない機能追加・名前変更・全体リファクタリングを混ぜない。整形は編集したファイルだけ。

## 統合と競合

- 作業ブランチは原則1人・1タスク。READMEで個人用の固定ブランチが指定されている場合は、その割り当てに従い、同じブランチで複数タスクを並行しない。別の人やAIと同じ作業ブランチを共有しない。
- 作業を保護してから最新のリモートを取得し、指定された統合ブランチを作業ブランチへ取り込む。
- 競合は双方の意図を確認してファイルごとに解消する。新しい方の丸ごと採用や、一括の `ours` / `theirs` で済ませない。
- 共通のモデル・依存関係・接続仕様で判断が必要な競合は、対象と選択肢を統合担当へ示す。独立して進められる作業は継続してよい。
- ロックファイルの競合は手で行を混ぜず、統合担当が依存宣言を解決し、指定環境で再生成して差分を確認する。
- force push、共有履歴のrebase、`reset --hard` で他人の変更を消さない。
- 統合後は再検証する。以前のコミットの成功結果では代用しない。
- PRは順番に統合する。先行PRが入ったら後続PRは統合先を再取得し、取り込み・検証・push・必須チェックをやり直す。競合なしでも接続仕様が合うことを確認する。

## 画像・フォントなどの素材

- READMEが指定する担当別の素材フォルダを使う。共通素材・素材登録設定・追加ライブラリは共通管理として扱う。
- 素材本体と、それを参照する実装を同じPRに含める。ローカルの絶対パスや未共有ファイルに依存させない。
- ファイル名は既存規約に合わせ、参照パスの大文字小文字も一致させる。同名ファイルの上書き・削除・移動は利用箇所を確認してから行う。
- 新しい形式やサブフォルダが自動で読み込まれるとは限らない。登録方法、対応形式、実際の表示を確認する。
- 画像などのバイナリ競合を自動採用で解消しない。双方の素材を確認し、残すファイルと参照先を調整する。
- 出典・利用条件・生成した場合の情報をプロジェクト指定の場所に残す。秘密情報や配布できない素材を含めない。
- 元の編集データや過大な素材を無条件に追加せず、用途に合った解像度・容量を選ぶ。担当外の素材を一括変換しない。

## 検証・PR・マージ

- READMEが案内する検証を実行する。変更に適したテストで確認し、実行できない項目は未実施と明記する。
- テストの削除・skip、解析除外、CIの削除・失敗無視、ブランチ保護の解除でチェックを通さない。
- `git diff --name-only` と `git diff --check` で担当外変更・競合マーカー・不用意な差分を確認。
- `git add ファイル名...` で対象を指定する。`git add .` / `git add -A` は使わず、ステージした差分を確認してコミットする。
- PRには担当範囲、変更前後の動作、共通ファイル変更の理由、検証結果と未確認事項を記載する。
- PRの作成前とマージ前に、リポジトリ・宛先・変更ファイル一覧を確認する。無関係なアプリや共通設定の大量差分があれば原因を調べ、宛先だけを機械的に変更しない。
- ページ単位のPRでよい。共有データ・保存・画面遷移への接続は完成前に確認し、必要なら接続が動く段階とページ完成の2段階に分ける。未完成で既存操作を壊す状態は統合しない。
- AI監査は人の承認やCIの代用にしない。最新のPR差分・接続先・関連テストを監査し、重大な指摘を解消してから人が仕様・操作を確認する。修正が入れば影響範囲を再監査・再検証する。
- マージ操作はPR番号と対象リポジトリを明示する。承認数・マージ方式はプロジェクトの資料と有効なGitHub設定の両方で確認する。
- マージの明示的な依頼がない場合はPR作成まで。依頼されている場合は最新の統合先を取り込み、最新コミットの必須チェック成功・競合なしを確認してマージする。
- 失敗・実行中・キャンセル・チェックなしは成功と扱わない。保護を迂回しない。
- 結果は変更箇所、検証、残る課題を短く報告する。実際に確認していない端末や環境まで動作保証しない。
