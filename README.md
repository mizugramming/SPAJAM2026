# つなぐん — honban の一台用デモ基盤

目の前の人と遊び、出会った相手が子分として自分のツナ缶に残る、イベント向けのアイスブレイクアプリです。最後は普通の子分＝3・骨＝1の戦力で二チームの綱引きを行います。

**現在は一台で画面・報酬・イベント進行を確認するデモです。正式なミニゲーム、端末間通信、QR・NFC、永続保存は未実装です。** 再起動するとデータは消えます。Androidが本番対象で、WebはPCでの画面確認に使います。

## 最初に読むもの

1. [共同開発ルール](AGENTS.md)
2. [今回の決定・暫定案・未実装](docs/decisions.md)：ユーザーの最新修正を含み、提供設計原本の食い違う箇所より優先します。
3. [端末と共通画面条件](docs/app_design.md)：REDMI Note 15 5G、スマホ縦向き・タッチ操作が基準です。
4. [提供された v2 設計原本](docs/tsunagun/APP_DESIGN.md)：原本を保持しているため、親分骨化など撤回済みの記述が残っています。
5. [構成・担当範囲と共通接続](docs/feature_integration.md)／[環境・検証・PR手順](docs/development.md)／[素材管理](docs/assets.md)

`docs/tsunagun/` は受領原本です。原本や `templates/project/` の雛形で、上記の最新決定や実装を上書きしません。

## 起動

このリポジトリの **`honban`** が本番統合先です。初回のFVM・Android環境準備は[開発手順](docs/development.md)を参照してください。取得済みの作業コピーのルートで実行します。

```bash
fvm install
fvm dart tool/check_environment.dart
fvm flutter pub get --enforce-lockfile
fvm flutter devices
fvm flutter run -d chrome
```

Androidでは環境チェックに `--android` を付け、`fvm flutter run -d 端末ID` で起動します。固定SDKは [.fvmrc](.fvmrc) と [tool/toolchain.json](tool/toolchain.json) が正本です。異なるSDKに合わせて制約やロックを変更しません。

## 一周を確認する

1. はだ缶の入口からルーム作成、またはデモコード `TSUNA` で参加する。
2. 空欄からニックネーム・趣味・ひとことを入力し、缶ラベルへの即時反映を確認する。
3. 開始待ちから交流を開始する。参加経路ではデモ操作で主催者の開始を再現する。
4. 「つながる」から仮想相手を選び、名前とチームを確認する。
5. 別チームなら対戦、同チームなら協力の空枠へ進む。デモ操作で勝敗・成功失敗を注入する。
6. 敗北では骨の子分を獲得し、協力成功で古い骨が普通へ成長する。親分は常に普通の姿のまま。
7. 「ホームに戻る」で缶へ帰り、子分から相手のプロフィールを振り返る。
8. 期限まで待つかデモ操作で期限へ進め、綱引き・MVP・ルーム順位を確認する。

ゲーム本体がないことを示したまま、結果とその後の体験を再現します。このデモコードで別の端末と同じルームに接続することはできません。

## 共同開発

- PR先は **`honban`**。`main` は再利用する開発キット、`rehearsal/02` は別アプリです。相互にブランチ全体をマージしません。
- 基盤・共通処理の統合担当は基盤作成者です。開発者と担当ファイルの割当はまだ未記入です。[担当表](docs/feature_integration.md)を埋めてから並行作業を始めます。
- 各担当は[開始・再開プロンプト](docs/ai_prompts.md)を使います。同じ画面・親ファイルを複数人が並行編集しないよう分けます。
- 共通の `PhoneViewport` を使用し、各画面が幅・高さ・倍率・向き制御を独自に変えません。PCプレビューは仮の最大412 × 900論理pxで、実機寸法ではありません。
- ページ単位のPRで構いません。共有状態との接続は早めに確認し、AI監査・最新CI・人の確認を経て順に統合します。AIは明示的なマージ依頼がない限りPR作成までです。
- CIのチェック名は `check` です。GitHubの `honban` 保護設定や必要承認数は資料追加で有効になりません。統合前に実際の設定を確認します。

## 検証と残る確認

[開発手順のローカルチェック](docs/development.md)を実行します。CIは文書・SDK整合、整形、解析、テスト、WebとAndroid debug APKのビルドを対象にします。チェックの定義と実行成功は別です。各PRに実行結果を残してください。

REDMI Note 15 5Gの実機論理サイズ、OS・文字サイズ設定、キーボード、実端末での操作・性能は未確認です。数十台の通信、保存復元、ゲームの面白さ、イベント参加者同士のアイスブレイク効果も、この一台デモだけでは検証できません。
