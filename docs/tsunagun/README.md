# つなぐん 設計書 v2

## 読む順番

1. `APP_DESIGN.md`：確定事項・暫定案・未決定事項・変更点、画面と操作の流れ。
2. `CODEX_START.md`：Codexへ渡す短い実装開始指示。
3. `references/SPAJAM.pdf` と本文中の画像：ユーザー提供の原図と参考イメージ。

## Codexへの渡し方

このフォルダをそのまま作業環境へ置き、`APP_DESIGN.md` を参照させてください。
`CODEX_START.md` の内容を開始指示として使用できます。

Markdownは画像を相対パスで参照しています。`APP_DESIGN.md` だけを移動すると画像が表示されないため、`assets/` と `references/` も同じ位置関係で渡してください。

## 注意

- 過去の生成画像は、最新仕様を反映した完成図ではありません。本文の変更点が優先です。
- 対戦・協力ゲームの本体は未決定です。
- 子分の重み「普通3・骨1」と、一台用デモの方針は過去の承認を維持しています。
- 本資料はアプリのビルド・通信・実機NFCを検証した成果物ではありません。
- 既存の `AGENTS.md` を置き換えるファイルは同梱していません。
