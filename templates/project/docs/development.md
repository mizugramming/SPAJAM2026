# 環境・開発手順

作成者がこのアプリで検証した設定とコマンドを記載します。バージョンは環境設定ファイルを正本とし、AIの共通ルールへ数値を転記しません。

## 固定する環境

| 項目 | バージョンの正本 | 導入・確認方法 |
|---|---|---|
| SDK・ランタイム | 未記入 | 未記入 |
| パッケージ管理ツール・依存ロック | 未記入 | 未記入 |
| 対象プラットフォームのビルド環境 | 未記入 | 未記入 |
| IDEが参照するSDK | 未記入 | 未記入 |

OSごとの追加準備：未記入。SDK本体・キャッシュ・端末固有パス・鍵はコミットしません。

## 初回準備と起動

新しくcloneした環境で検証した、次のコマンドを実行順に記載します。

1. 指定SDKの導入とバージョン確認：未記入。
2. ロックを守った依存取得：未記入。
3. 外部サービスなどの設定：未記入。不要なら「なし」。
4. アプリの起動と対象端末の選択：未記入。
5. 起動後に確認する操作：未記入。

## 日々の更新と共有前の検証

READMEの統合先と作業ブランチを使います。未コミット変更を保護してから、自分のブランチを更新し、最新の統合先を取り込みます。

- 日々の更新コマンド：未記入。
- 環境・ロック整合性のチェック：未記入。
- 整形・静的解析：未記入。
- テスト：未記入。
- 対象プラットフォームのビルド：未記入。
- 実機・ブラウザで確認する操作：未記入。

競合はファイルごとに双方の意図を確認して解消します。SDK・依存関係・ロックは各自のエラー回避のために変更しません。

## CI・PR・マージ

- アプリ用CIのファイル：未記入。
- ローカルと同じバージョンを読む方法：未記入。
- 必須チェック名：未記入。
- レビューと例外権限の方針：未記入。
- GitHub上の保護設定を適用・確認した日：未記入。

キットの文書チェックCIを、そのままアプリの動作確認として使わないでください。アプリ用の解析・テスト・ビルドを実行するCIを用意します。保護設定はJSONなどを配置するだけでは有効になりません。

PRは順番に統合し、先行PRが入ったら後続PRは取り込み・再検証を行います。公開・リリースに追加作業が必要な場合の手順：未記入。

## 基盤引き渡し時の確認

- 本番用リポジトリURL・統合先・基盤コミット：未記入。
- 各担当の取得先・作業フォルダが正しいことを確認する手順：未記入。
- GitHubとCLIの認証・push権限を確認する手順：未記入。秘密情報の値は共有しない。
- 統合担当／統合担当自身のPRを承認する人：未記入。
- マージ方式と作業ブランチ削除方針：未記入。番号ブランチ継続利用ならmerge commit方式と自動削除無効を確認する。

| 担当者 | 基盤コミット | SDK・対象端末 | 起動・接続・保存復元の結果 | 未解決事項 |
|---|---|---|---|---|
| 未記入 | 未記入 | 未記入 | 未記入 | 未記入 |

全員が同じ基盤を起動してから担当作業へ進みます。環境差をSDK制約や依存変更で回避せず、統合担当が原因を確認します。

## PRの大きさと監査

ページ単位のPRでよく、細かい見た目の修正ごとの分割は不要です。共有データ・保存・画面遷移との接続が不確かな場合は「最小の接続が動く段階」と「ページ完成」に分けます。既存操作を壊す未完成コードはマージせず、必要ならDraft PRで共有します。

通常PRは作成者以外の人1人の承認を基本案とします。採用する人数は上のCI・PR・マージ欄に記入し、GitHubの有効なルールと一致させます。共通モデル・保存形式・依存関係の変更は、影響する担当にも確認を依頼します。AI監査は人の承認とCIの代用にしません。

監査担当のAIには、対象リポジトリとPR番号を添えて次を渡します。監査だけでは承認の投稿やマージは許可しません。

```text
このPRを読み取りで監査してください。AGENTS.mdとREADME、設計・接続・開発資料を読み、
PRの宛先、最新の差分、実際の接続先と関連テスト、CI結果を確認してください。
担当外変更、保存形式・共通APIの不整合、入力消失・二重保存、エラー時の挙動、
素材の参照漏れ、未検証の主要操作を確認してください。
指摘は重要度、ファイル・行、問題が起こる条件、影響、修正案を示してください。
問題を見つけなかった場合も監査したコミットと未確認事項を報告してください。
ファイル変更・承認の投稿・マージは行わないでください。
```

重大な指摘を修正し、最新の差分を再監査・再検証してから人が仕様と操作を確認します。AIの推測は再現条件やコードで確かめ、指摘を無条件に採用しません。

## マージ直前と発表前

- 統合担当がリポジトリ・PR番号・宛先・変更一覧を確認する。宛先違いや大量の無関係な差分があれば、原因と修正後の差分を確認する。
- 先行PRが入ったら後続は最新の統合先を取り込み、依存取得・検証・push・監査と承認の確認をやり直す。
- 最新コミットの必須チェック・必要な人の承認・競合なし・レビュー会話の解決を確認する。チェックなしや必要な検証のskipを成功扱いにしない。
- マージは対象リポジトリとPR番号を明示し、1件ずつ行う。所有者も通常は保護を迂回しない。
- マージ後は統合ブランチのCI結果と主要操作を確認し、全員が統合版を取り込む。不具合が出た場合は追加の統合を止め、統合担当が修正PRまたは取り消しPRを判断する。共有履歴をresetしない。

発表前の追加実装を区切る時刻・担当：未記入。
デモ端末での起動から主要操作完了までの確認手順：未記入。
確認済みコミット・データ準備・既知の制約：未記入。
ネットワークや外部サービスが使えない場合のデモ方針：未記入。不要なら「対象外」と理由を記載。
確認後の修正も再検証し、以前の成功だけで発表用へ反映しません。
